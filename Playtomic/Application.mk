APP_ABI := armeabi armeabi-v7a
APP_PROJECT_PATH := $(call my-dir)
NDK_PROJECT_PATH := APP_PROJECT_PATH
APP_BUILD_SCRIPT := $(call my-dir)/Android.mk
APP_MODULES := Playtomic
#APP_STL := stlport_static